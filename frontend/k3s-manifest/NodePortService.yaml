apiVersion: v1
kind: Service
metadata:
  name: fridge-log-frontend-service
spec:
  type: NodePort
  selector:
    app: fridge-log-frontend
  ports:
    - port: 80
    - targetPort: 3000
    - nodePort: 30007
